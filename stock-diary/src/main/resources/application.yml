apple:
  client-id: ${APPLE_CLIENT_ID:your.service.id.web}  # Service ID (웹용)
  app-id: ${APPLE_APP_ID:your.app.bundle.id}  # App Bundle ID (iOS용)
  redirect-uri: ${APPLE_REDIRECT_URI:http://localhost:8080/api/v1/auth/apple/callback}
  team-id: ${APPLE_TEAM_ID:YOUR_TEAM_ID}
  key-id: ${APPLE_KEY_ID:YOUR_KEY_ID}
  private-key: ${APPLE_PRIVATE_KEY:BASE64_ENCODED_PRIVATE_KEY}
  key-file-path: ${APPLE_KEY_FILE_PATH:AuthKey_WCY87M37P6.p8}

kakao:
  client-id: ${KAKAO_CLIENT_ID}
  client-secret:
  redirect-uri: ${KAKAO_REDIRECT_URI:http://fodiy.iptime.org:28080/api/v1/auth/kakao/callback}
  admin-key: ${KAKAO_ADMIN_KEY}

jwt:
  secret-key: ${JWT_SECRET_KEY}
  access-token-expiration: 120000       # 2분초 (밀리초)
  refresh-token-expiration: 12000000      # 200분 (밀리초)

# Cloud Storage 설정 (MinIO / AWS S3)
cloud:
  storage:
    endpoint: ${STORAGE_ENDPOINT}
    access-key: ${STORAGE_ACCESS_KEY}
    secret-key: ${STORAGE_SECRET_KEY}
    region: ${STORAGE_REGION:us-east-1}
    bucket: ${STORAGE_BUCKET:depromeet}
    # AWS S3로 전환시: endpoint를 제거하고 region만 사용 (예: ap-northeast-2)

spring:
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 30MB
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
#        dialect: org.hibernate.dialect.H2Dialect
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  h2:
    console:
      enabled: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs

server:
  url: ${SERVER_URL:http://localhost:8080}

#logging:
#  level:
#    com.dpm05.user: DEBUG
#    org.springframework.security: DEBUG

logging:
  level:
    com.dpm05.user: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web.reactive.function.client.ExchangeFunctions: DEBUG
    com.ogd.stockdiary.application.batch: DEBUG